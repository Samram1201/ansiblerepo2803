pipeline {
    agent any
    stages {
        stage('Get_Playbook') {
            steps {                
                git 'https://github.com/Samram1201/ansiblerepo2803.git'
            }
        }
        stage('Run_Playbook') {
            steps {
                sh 'ansible-playbook playbook.yml'
            }
        }
    }
}
